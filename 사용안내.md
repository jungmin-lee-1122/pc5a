# 5A 아카데미 홈페이지 — 사용 안내

Next.js 16 + React 19 + Tailwind v4 로 만든 학원 홈페이지입니다.
별도 DB 없이 `data/*.json` 파일에 콘텐츠를 저장하며, 관리자 페이지에서 수정합니다.

## 실행

```bash
npm run dev      # 개발 서버 (http://localhost:3000)
npm run build    # 프로덕션 빌드
npm run start    # 프로덕션 실행
```

## 관리자 페이지

- 주소: `http://localhost:3000/admin`
- 기본 비밀번호: `admin1234`
- 비밀번호 변경: 프로젝트 루트에 `.env.local` 파일을 만들고 아래를 넣으세요.

```
ADMIN_PASSWORD=원하는비밀번호
ADMIN_SECRET=아무거나-긴-랜덤문자열
```

관리자에서 관리하는 항목: 히어로 슬라이드 · 포스터 · 합격실적 · 강사진 · 공지사항 ·
입시설명회 · 영상 · 홍보 배너 · 사이트 정보(헤더/푸터/과목 탭/소셜).

각 항목은 순서 변경(↑↓), 수정, 삭제, 새로 추가가 가능하며,
이미지는 "파일 선택"으로 업로드하면 `public/uploads/` 에 저장되고 자동으로 경로가 채워집니다.

## 이미지

- 지금은 `public/placeholders/` 의 임시 SVG가 들어가 있습니다.
- 실제 이미지는 관리자에서 업로드하거나, `public/` 에 넣고 경로(`/파일명`)를 입력하면 됩니다.
- 영상은 유튜브 영상 ID 또는 URL만 넣으면 썸네일이 자동으로 표시됩니다.

## 코드에서 직접 고치는 것

- **상단 메뉴 구조**(학원소개/모집안내 등 카테고리·세부항목): `lib/nav.ts`
  여기만 고치면 호버 드롭다운과 전체 메가메뉴에 동시에 반영됩니다.
- **색상/폰트 등 디자인 토큰**: `app/globals.css` 상단 `@theme` 블록
- **각 섹션 컴포넌트**: `app/components/`

## 폴더 구조 요약

```
app/
  page.tsx              메인 페이지 (섹션 조립)
  layout.tsx            공통 레이아웃
  globals.css           디자인 토큰 + 기본 스타일
  components/           프론트 섹션 (Header, Hero, Teachers, ...)
  components/admin/     관리자 공통 컴포넌트
  admin/                관리자 페이지
  api/                  데이터 API (CRUD) + 로그인/업로드
lib/                    타입, 데이터 스토어, 인증, 네비 설정
data/                   콘텐츠 JSON (관리자가 수정)
public/placeholders/    임시 이미지
public/uploads/         관리자 업로드 이미지 (git 제외)
```

## 배포 시 주의

콘텐츠가 서버의 `data/*.json` 파일에 저장되므로, 파일 쓰기가 가능한 환경
(일반 서버 / 직접 호스팅)에 배포하세요. Vercel 같은 서버리스 환경은 실행 중
파일 저장이 유지되지 않으므로 이 방식과는 맞지 않습니다(추후 DB 연동 필요).
