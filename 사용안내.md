# 5A 아카데미 홈페이지 — 사용 안내

Next.js 16 + React 19 + Tailwind v4. 별도 DB 없이 동작합니다.

## 실행
```bash
npm run dev      # 개발 서버 (http://localhost:3000)
npm run build    # 프로덕션 빌드
npm run start    # 프로덕션 실행
```

## 무엇을 어디서 수정하나 (중요)

### 1) 관리자 페이지에서 수정  →  http://localhost:3000/admin
로그인 후 GUI로 추가/수정/삭제/순서변경 (데이터는 `data/*.json`에 저장)
- **강사진** (과목·이름·태그·사진, 사진 업로드 지원)
- **공지사항**
- **입시설명회** (설명회/입시교실 분류)
- **영상** (유튜브 ID 또는 URL만 입력하면 썸네일 자동)

기본 비밀번호 `admin1234` → 변경하려면 루트에 `.env.local` 생성:
```
ADMIN_PASSWORD=원하는비밀번호
ADMIN_SECRET=아무거나-긴-랜덤문자열
```

### 2) 코드에서 직접 수정  →  config/homepage.ts
아래 항목은 이 파일 하나에서 값만 바꾸면 됩니다.
- **SLIDES** — 히어로 롤링 슬라이드
- **POSTER** — 히어로 우측 포스터
- **BANNER** — 롤링창·포스터 바로 밑 배너 박스 (이미지 하나)
- **PROMO** — 영상 섹션 우측 하단 홍보 배너
- **SITE** — 헤더 브랜드, 강사 과목 탭, 푸터 정보, 소셜/상담 링크

이미지는 `public/` 폴더에 넣고 `/파일명` 으로 경로를 적습니다.
(예: `public/slide-a.jpg` → `image: "/slide-a.jpg"`)
현재는 `public/placeholders/`의 임시 SVG가 들어가 있습니다.

### 3) 그 외 코드 위치
- **상단 메뉴 구조**(카테고리/세부항목): `lib/nav.ts`
- **색상/폰트 토큰**: `app/globals.css` 상단 `@theme`
- **로고 이미지**: `public/logo.png`
- **각 섹션 컴포넌트**: `app/components/`

## 폴더 구조
```
config/homepage.ts   ← 슬라이드·포스터·배너·홍보배너·사이트정보 (직접 수정)
lib/nav.ts           ← 상단 메뉴 구조
app/page.tsx         ← 메인 페이지 조립
app/components/       ← 프론트 섹션
app/admin/           ← 관리자 페이지 (강사/공지/설명회/영상)
app/api/             ← 데이터 API + 로그인/업로드
data/                ← 관리자 콘텐츠 JSON (teachers/notices/events/videos)
public/              ← 이미지 (logo.png, placeholders/, uploads/)
```

## 저장소 구조 (중요)

관리자 콘텐츠(선생님·공지·설명회·영상)와 업로드 이미지의 저장 위치가 환경에 따라 자동으로 바뀝니다.

- **로컬(`npm run dev`)**: `data/*.json` 파일 + `public/uploads/` (설정 불필요, 그대로 동작)
- **Vercel 배포**: Upstash Redis(데이터) + Vercel Blob(이미지)

Vercel은 실행 중 파일 쓰기가 안 되므로, 배포에서 관리자 저장을 쓰려면 아래 저장소를 한 번 연결해야 합니다.

## Vercel 설정 (최초 1회)

1. **Upstash Redis 연결** — Vercel 프로젝트 → **Storage** 탭 → **Create Database** → **Upstash for Redis** 선택 → 이 프로젝트에 연결.
   (연결 시 `KV_REST_API_URL`, `KV_REST_API_TOKEN` 환경변수가 자동 주입됩니다.)
2. **Vercel Blob 연결** — 같은 **Storage** 탭 → **Blob** 생성 → 이 프로젝트에 연결.
   (`BLOB_READ_WRITE_TOKEN` 자동 주입.)
3. **관리자 비밀번호 설정** — Settings → Environment Variables 에 추가:
   - `ADMIN_PASSWORD` = 원하는 관리자 비밀번호
   - `ADMIN_SECRET` = 아무거나 긴 랜덤 문자열(세션 서명용)
4. **재배포** — 위 설정 후 Vercel에서 **Redeploy** (또는 git push).

이후 `https://(사이트)/admin` 에서 로그인 → 저장·사진 업로드가 정상 동작합니다.
처음엔 시드(기본) 데이터가 보이고, 관리자에서 저장하는 순간부터 Redis 값이 사용됩니다.

## 로컬에서 다시 실행할 때
새 패키지가 추가됐으니 한 번만:
```bash
npm install
npm run dev
```
